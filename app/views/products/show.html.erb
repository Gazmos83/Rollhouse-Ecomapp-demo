<div class="index-content">
  <div class="container">
  <div class="row product" data-product-id="<%= @product.id %>">

    <div class="col-sm-12 col-md-6 col-lg-6">
      <%= image_tag(@product.image_url, width: "350", class: "img-fluid img-zoom", data_zoom_image:"@product.image_url") if @product.image_url%>
      <%= link_to 'Edit', edit_product_path(@product) %>
      <%= link_to 'Back', products_path %>

    </div>
    <div class="col-sm-12 col-lg-6">
    <%= render 'shared/new_comment' %>
    <%= form_with(url: '/payments/create') do |form| %>
       <%= render partial: "shared/stripe_checkout_button" %>
       <%= hidden_field_tag(:product_id, @product.id) %>
     <% end %>
    </div>
    <div class="col-sm-12 col-md-6 col-lg-6 productinfo-right">
      <% cache @product do %>
      <p>
        <%= @product.name %>
      </p>
      <p>
        <%= @product.description %>
      </p>
      <% end %>
      <p class="text-bold">Overview</p>
      <p class="product-galleryprice-tag">
        <%= number_to_currency(@product.price, :unit => "€") %>
      </p>

      <div class="star-rating">
      <p>Review score:<div id="average-rating" class="rated" data-score="<%= @product.average_rating %>"></p>
        </div>
        <% if !@product.highest_rating_comment.nil? %>
        <%= @product.highest_rating_comment.body %>
        <% end %>

        <%= render 'shared/comments' %>
      </div>
    </div>

   </div>
  </div>

</div>
<script src="jquery.elevatezoom.js" type="text/javascript"></script>
